/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/lsb-thompson-real-estate-company-tallahassee',
  images: { unoptimized: true },
};
module.exports = nextConfig;
